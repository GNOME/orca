# user-settings.py - custom Orca settings
# Initially generated by orca-setup

import re

import orca.debug as debug
from orca.acss import ACSS

debug.setDebugLevel(debug.LEVEL_INFO)
f = open('%debug%', 'w', 0)
debug.setDebugFile(f)

speechServerFactory = 'orca.gnomespeechfactory'
speechServerInfo = ['Fonix DECtalk GNOME Speech Driver', 'OAFIID:GNOME_Speech_SynthesisDriver_Dectalk:proto0.3']
voices = {
     'default' : ACSS({'family': {'locale': 'en_US', 'gender': None, 'name': 'Paul'}}),
     'uppercase' : ACSS({'average-pitch': 6}),
     'hyperlink' : ACSS({'average-pitch': 4}),
}

useKeyEcho = False
useSpeech = True
useBraille = True
