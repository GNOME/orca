# Default settings.py -- this is used when the user has no settings.py
# or when the user has no home directory as in the case of gdm

# We don't know what engines are installed, so voices is empty

voices = {}
keyEcho = False
useSpeech = True
useBraille = False
